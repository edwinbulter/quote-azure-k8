apiVersion: v1
kind: Service
metadata:
  name: quote-app-service
  namespace: quote-app
spec:
  selector:
    app: quote-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer
